// create a function that will take arr1 values and assign them to the keys, and arr2 values as the values associated with those keys.

function Zipper(arr1, arr2) {
	var obj = {};
	for (var i = 0; i < arr1.length; i++){
		obj[arr[i]] = arr2[i];
	}
	}
}