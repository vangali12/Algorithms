

			movement = 1;
			return false;
			return helper (idx + movement, length/2);
			return helper(idx/2, length/2);
			return idx;
		idx = Math.floor(idx);
		if (list[idx] == val) {
		if (movement < 1) {
		length = Math.floor(length);
		return false;
		var movement = length/4;
		}
		}
		} else if (length == 1) {
		} else if (list[idx] < val) {
		} else if (list[idx] > val) {
	function helper(idx, length) {
	if (list.length == 0) {
	return helper(list.length/2, list.length);
	}
	}
-
arr = [1, 4, 8, 9, 11, 16, 18, 19, 25, 28];
console.log(bSearch(100, arr))
console.log(bSearch(2, arr));
console.log(bSearch(28, arr))
console.log(bSearch(8, arr));
function bSearch(val, list) {
}